// App version
export const CURRENT_VERSION = '/v1';

// For cache expiration. Bump this number by 1 when you want to invalidate
// every user's cache, e.g. you changed all the videos in s3.
export const VIDEO_VERSION = '1';
